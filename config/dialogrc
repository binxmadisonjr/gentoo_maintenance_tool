# Run-time configuration file for dialog
# Automatically generated and updated for improved modular use

# Screen aspect ratio (0 = default)
aspect = 0

# Widget separator
separate_widget = ""

# Tab length for textbox conversion
tab_len = 8

# Enable tab-traversal in checklists
visit_items = ON

# Enable scrollbar
use_scrollbar = ON

# Enable shadow effects
use_shadow = ON

# Enable colors
use_colors = ON

# General screen background and dialog box colors
screen_color = (CYAN,BLUE,ON)
dialog_color = (BLACK,WHITE,OFF)

# Title and border colors
title_color = (BLUE,WHITE,ON)
border_color = (WHITE,WHITE,ON)

# Button colors
button_active_color = (WHITE,BLUE,ON)
button_inactive_color = dialog_color
button_key_active_color = button_active_color
button_key_inactive_color = (RED,WHITE,OFF)
button_label_active_color = (YELLOW,BLUE,ON)
button_label_inactive_color = (BLACK,WHITE,ON)

# Input box colors
inputbox_color = dialog_color
inputbox_border_color = border_color

# Menu box and items
menubox_color = dialog_color
menubox_border_color = border_color
item_color = dialog_color
item_selected_color = button_active_color
tag_color = title_color
tag_selected_color = button_label_active_color

# Progress and gauge colors
gauge_color = title_color

# Arrows and help text
uarrow_color = (GREEN,WHITE,ON)
darrow_color = uarrow_color
itemhelp_color = (WHITE,BLACK,OFF)
